// symple arrow function 

let name = (uname) => {
    return `hey ${uname}` 
}
console.log(name("ajay"))

// we can define one more way 

let age = (age1) => age1+1
console.log(age(20)) 

// in arrow fun u must return value if you are using {}
// in arrow fun u must return is not require   if you are using ()